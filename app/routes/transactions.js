import Ember from "ember";
// import AuthenticatedRouteMixin from 'ember-simple-auth/mixins/authenticated-route-mixin';
import AuthRoute from "pie/mixins/auth-route";

export default Ember.Route.extend(/*AuthenticatedRouteMixin, */ AuthRoute, {});
