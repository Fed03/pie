<header>
  <nav class="mui-appbar">
    <span class="appbar-brand appbar-title mui--appbar-line-height">Pie</span>
    {{#if session.isAuthenticated}}
      <span class="appbar-brand mui--appbar-line-height" data-test-user-balance>
        {{format-money currentUser.user.currentBalance}}
      </span>
    {{/if}}
  </nav>
</header>
<div class="mui--appbar-height"></div>
<main>
  <div class="mui-container-fluid">
    <div class="mui-row">
      {{outlet}}
    </div>
  </div>
</main>
