<span data-test-value-display {{action "toggleModal"}}>
  {{displayValue}}
</span>
{{#if label}}
  <label>{{label}}</label>
{{/if}}
{{#if modalOpened}}
  {{#modal-dialog onClose="toggleModal" translucentOverlay=true}}
    {{calc-input value=internalValue onEquals=(action 'calc')}}
  {{/modal-dialog}}
{{/if}}
